# uv-managed virtual environment
.venv/
__pycache__/

# SkillOpt runtime state — regenerated per project; keep only the placeholder
.claude/skillopt/*
!.claude/skillopt/.gitkeep

# commit uv.lock for reproducibility (do NOT ignore it)
